server:
  port: 9003

spring:
  application:
    name: back-hall
  cloud:
    config:
      discovery:
        enabled: true
        service-id: BACK-CONFIG
      profile: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/
