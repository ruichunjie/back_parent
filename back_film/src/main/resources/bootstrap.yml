server:
  port: 9004

spring:
  application:
    name: back-film
  cloud:
    config:
      discovery:
        enabled: true
        service-id: BACK-CONFIG
      profile: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/
